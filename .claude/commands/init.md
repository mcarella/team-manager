# /init — Load Project Context

Carica il contesto completo del progetto team-manager.

## Steps

1. Leggi `.claude/CLAUDE.md` — architettura e domain model
2. Leggi `packages/shared/src/types.ts` — interfacce TypeScript
3. Leggi `packages/team-manager-core/src/index.ts` — entry point domain logic
4. Esplora struttura `apps/` con @explorer
5. Riporta:
   - Stack attivo
   - Stato implementazione (cosa c'è, cosa manca)
   - Prossimo prompt da PROMPTS-TDD.md

Sei pronto per `/implement`.
